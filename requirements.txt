# Core framework - stable on Python 3.12
aiogram

# Config & validation

pydantic-settings

# Database
aiosqlite

# Scheduling
APScheduler

# Fuzzy search (pure Python fallback for compatibility)
fuzzywuzzy
python-Levenshtein; sys_platform != "win32"

# AWS & HTTP
boto3
aiohttp

# Utilities
python-dateutil
pyyaml
psutil

# Windows compatibility
colorama